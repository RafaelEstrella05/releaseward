apiVersion: v1
kind: Service
metadata:
  name: releaseward-demo
  namespace: releaseward-dev
  labels:
    releaseward.dev/environment: development
spec:
  selector:
    app: releaseward-demo
  ports:
    - port: 80
      targetPort: 3000
